# Session handoff — 2026-07-03 (supersedes 2026-07-02-handoff.md)

> Superseded on 2026-07-09. The three bot fixes described below were implemented
> and merged; the production bot later gained obligations, inline controls,
> per-chat serialization, and AI telemetry. Use the trailing current-state block
> in `thoughts/notes/PROGRESS.md`, not the actions in this handoff.

**Branch:** `feat/family-finance-mvp` (PR #2) · **Latest pushed:** `1bb5281` · **Ahead of origin:** 0 (all pushed)
**Progress log:** `thoughts/notes/PROGRESS.md` — read the trailing "Current state" first.

## Where you are

v1.0 is **live in production** (web on Vercel `https://casa.alvaroekarol.com.br`, bot container on the VPS, Supabase self-hosted, Telegram + audio working, model = Haiku 4.5). This session shipped UI polish, the Haiku switch, the live deploy, and a bot save-error fix. The user then reported **3 more bot bugs**, approved a plan to fix them, and asked for a handoff. **The 3 bugfixes are NOT started** — that's the next work.

## Next action

Implement the 3 approved bot bugfixes (TDD, in `apps/bot`), then rsync-redeploy the bot. Start with the responsible-default change in `apps/bot/src/conversation.ts` `startConversation` (~line 258): default `draft.responsibleUserId = input.fromUserId`.

## The approved plan (decisions already made with the user)

1. **Responsável padrão = the Telegram sender.** Today default responsibility is the house (`undefined`) unless a name is said ([conversation.ts:187,258-265](../../apps/bot/src/conversation.ts)). Change: default `responsibleUserId = input.fromUserId`. Keep overrides — "responsável Karol" → Karol; "responsável casa"/"casa" → house (`undefined`) via `resolveResponsibleUserId` (no member named "casa" → undefined). Confirmation summary then shows the sender's name. Tests: default=sender, "casa"→house, name→person.

2. **Always run the Haiku interpreter for description + category.** Root cause of "não fez parse com IA": the LLM runs ONLY when the deterministic parser finds no amount ([conversation.ts:238](../../apps/bot/src/conversation.ts)). "Gasto em OpenAI no valor de 56 reais e 13 centavos" has an amount → parser wins → crude description "Gasto OpenAI valor". Change: run `interpretText` **always** (when configured); use its clean `description` + `category_hint`; keep the deterministic parser as the amount/date source (LLM as fallback). **Tune the interpret prompt** ([interpret.ts](../../apps/bot/src/interpret.ts)) to extract just the merchant ("OpenAI", not "Gasto OpenAI valor"). **Fix `needsAttention`** ([conversation.ts:250-256](../../apps/bot/src/conversation.ts)) — it currently trips on `interpreted !== null`, which would now be always-true; keep it only for audio + uncertain amount/date. User goal: description clean + category easy to pick or just confirm.

3. **Strip leading/trailing punctuation from description.** "Tabacaria," → "Tabacaria". The parser cleaner ([parser.ts:193-200](../../apps/bot/src/parser.ts)) strips prepositions + "reais/R$" but not punctuation. Add a trim of leading/trailing punctuation, and apply as a final normalization on whichever description is used (parser or LLM).

**Tarefa C (light UX):** with better description the category suggestion improves; verify the confirmation shows the suggestion and "categoria X"/confirmar works. No new picker unless the user asks.

## In-flight state

- Working tree **clean**, everything pushed. No uncommitted edits except this handoff + PROGRESS.md (thoughts/ is local).
- No background processes needed. Local Supabase/web dev may or may not be up — not required for bot work (bot unit tests are pure, no network).

## Don't-forget context

- **VPS `/opt/family-finance` is NOT a git repo — deploy is rsync from local**, then `cd deploy/bot && docker compose build && docker compose up -d`. rsync command used this session (run from repo root):
  `rsync -az --exclude='.git' --exclude='node_modules' --exclude='.turbo' --exclude='.vercel' --exclude='.env' --exclude='.env.*' --exclude='*.tsbuildinfo' --exclude='.DS_Store' <repo>/ minesupply:/opt/family-finance/`
- **NEVER rsync over `deploy/bot/.env`** (excluded above) — it holds the Supabase service-role key + OpenAI/Anthropic/Telegram keys. It already pins `ANTHROPIC_MODEL=claude-haiku-4-5`.
- SSH alias **`minesupply`** = mine-ops = `2.24.71.244` (Hostinger). `docker logs -f family-finance-bot`; health `curl -s localhost:8787/health`.
- User debugging style: wants **intuitive pt-BR error messages** with the exact failing field (not raw Zod). Confirmed by the `1bb5281` fix.
- The bot uses the **service-role** Supabase client; `createTransactionDraft` (domain) is the single validation choke point shared by web + bot.
- Push/deploy: user has authorized push + prod deploy this session, but **confirm before each new deploy** — they chose production explicitly last time.

## Verification commands

```bash
# bot unit tests (pure, fast) — must stay green
pnpm --filter @family-finance/bot test
pnpm --filter @family-finance/bot typecheck
# after deploy:
ssh minesupply 'curl -s localhost:8787/health && docker logs --since 60s family-finance-bot'
```

## Reading order for the next agent

1. `thoughts/notes/PROGRESS.md` — trailing "Current state" block, then the 2026-07-03 session.
2. This doc — the 3-bug plan + decisions + rsync deploy recipe.
3. `apps/bot/src/conversation.ts`, `parser.ts`, `interpret.ts` — the files to change.
4. `thoughts/tech-debt.md` — open items.
