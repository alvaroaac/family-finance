# Session handoff — 2026-07-02 (supersedes 2026-07-01-handoff.md)

**Branch:** `feat/family-finance-mvp` · **Latest:** `feb913d` · **Ahead of origin:** 39 commits (**unpushed — permission required**)
**Progress log:** `thoughts/PROGRESS.md` — read the trailing "Current state" first.

## Where you are

**All four v1.0 phases are BUILT and reviewed.** This session: imported the user's Claude-design
mockups (`docs/design/2026-07-02-claude-design-mockups/`, commit `5d78787`) and executed Phase 1
(UI foundation) from plan `101268f` — 7 tasks, all first-pass review-clean, final whole-phase review
APPROVE. Gate re-verified independently: typecheck 12/12, **276 tests**, web build + lint clean,
zero old-skin colors left.

The design system is **plug-and-play by construction**:
- Tokens ONLY in `apps/web/app/globals.css` (both themes + semantics/washes/radii/shadows).
- Component CSS ONLY in `apps/web/components/ui/ui.css` (`.ff-*`).
- Primitives in `apps/web/components/ui/` take props/children/injected-actions only — the
  **`ui-firewall.test.ts`** fails the build if any ui/ file imports `@family-finance/*`, `lib/`,
  `app/`, or `next/headers|cache`. Never weaken that test.
- Pages own 100% of queries/actions; re-skins were markup-only (reviewed per task).

## Next actions — user's

1. **Eyeball the UI**: localhost:3000 (Google login works on the local stack). Both themes via the
   floating "Estilo" picker; mobile via responsive mode (bottom nav at ≤720px).
2. **Live deploy** per `deploy/README.md` (VPS Supabase → migrations → seed + Karol's email in
   `allowed_emails` → rls-proof gate → Vercel → Google OAuth prod → bot compose → setWebhook → smoke).
   Fill both members' Telegram IDs in /settings so the bot recognizes them.
3. ~~Push~~ DONE 2026-07-02: `feat/family-finance-v1` pushed, PR #2 (stacked on #1) open — https://github.com/alvaroaac/family-finance/pull/2. Local `feat/family-finance-mvp` ref itself still unpushed (PR #1 keeps its old tip).

## In-flight state

- **Uncommitted:** `supabase/config.toml` (Google provider block, carried since 06-30, safe, undecided).
- **Local stack:** 12 Supabase containers running; **web dev server RESTARTED 2026-07-02** (old one
  500'd — predated next/font; log at `/tmp/ff-web-dev.log`). If pages 500 after big web changes,
  restart the dev server before debugging.
- **NEVER `supabase db reset`** (live walkthrough + real fatura data). Use `supabase migration up`.

## Don't-forget context

- Phase-1 scope cuts (design shows data we don't track — future features, not bugs): account balances
  on Contas, "último aporte" on caixinhas, per-category monthly totals, manual-entry FAB/"+ Novo
  lançamento", @username badges (we store numeric telegram ids).
- Final-review non-blocking notes: Resumo kicker uses "<mês de ano>" instead of the mockup's live
  weekday date; `(app)/layout.tsx` gained a sanctioned `signOutAction` ("sair").
- Bot env: use `getBotServerEnv()` — plain `getServerEnv()` crashes the container (web-only vars).
- Bot persists `input_kind` as `"text" | "audio"`.
- Tech debt still open: #9 extensionAlias/dist, #11 XLSX variants, #14 live-API parsing,
  allowed_emails.household_slug unused (minor).
- DesignSync tool is main-loop-only (subagents can't call it); >32KB results persist to tool-results
  files, smaller ones must be extracted from the session `.jsonl`.

## Verification commands

```bash
cd /Users/alvarocarvalho/desenv/personal/alvaro-e-karol/family-finance
git log --oneline 1d62581..HEAD            # this session's 9 commits (mockups → phase 1)
pnpm typecheck && pnpm test                # 12/12, 276 tests (incl. ui-firewall)
pnpm --filter @family-finance/web build && pnpm --filter @family-finance/web lint
grep -rn "#11271f\|#e9f5ef" apps/web/app apps/web/components | wc -l   # 0
curl -s -o /dev/null -w "%{http_code}" localhost:3000/login             # 200
```

## Reading order for the next agent

1. `thoughts/PROGRESS.md` — trailing "Current state" + the 2026-07-02 section.
2. This doc.
3. `docs/superpowers/plans/2026-07-02-phase-1-ui-foundation.md` — what was built + the firewall rules.
4. `deploy/README.md` — the deploy runbook (the only unexecuted work, user-gated).
5. `memory/project-tech-debt.md` — open items.
