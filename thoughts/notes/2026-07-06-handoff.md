# Session handoff — 2026-07-06

**Branch:** `feat/bot-inline-buttons` · **Latest pushed:** `ed5f4f3` (PR #6 head on origin is `ed5f4f3`; local is 2 ahead) · **Ahead of origin:** 2 commits (`5c45bc5`, `b177bbc` — PR-2 spec docs)
**Progress log:** `thoughts/notes/PROGRESS.md` — read the trailing "Current state" first.
**SDD ledger (buttons feature):** `.superpowers/sdd/progress.md` — per-task commits + deferred minors.

## Where you are

The bot inline-buttons feature is code-complete on PR #6 (open, user must merge; the auto-mode classifier blocks agent self-merge). The follow-up feature (PR-2: card installments + card-bill payment via bot) has an **approved design spec** that survived a codex (GPT 5.5) review; the next artifact is its implementation plan.

## Next action

Invoke `superpowers:writing-plans` on
`docs/superpowers/specs/2026-07-06-bot-card-installments-and-bill-payment-design.md`,
save the plan to `docs/superpowers/plans/`, then STOP for the user's explicit
plan approval + execution-mode choice (their global rule — never combine).

## What just happened

- Buttons plan executed via subagent-driven development: 10 tasks, all reviews Approved; final whole-branch review caught a save-after-send double-insert regression → fixed `86e9e6a`. Full gate green.
- `feat/bot-inline-buttons` branched, origin/mvp merged in (test-file conflict resolved keep-both), pushed, PR #6 opened → `feat/family-finance-mvp`.
- PR-2 brainstormed with the user: bill payment = single `transfer` row, both instruments, `bill_month`, `settle_card_bill` RPC (approach A; decisions in PROGRESS.md).
- Spec written (`5c45bc5`), codex-reviewed → 6 real findings fixed (`b177bbc`), then approved via plan-review UI.
- codex-review skill docs fixed (`~/.claude/skills/codex-review/SKILL.md`): scope flags are mutually exclusive with a custom prompt on codex-cli 0.142.3.

## In-flight state

- **PR #6 unmerged** — user action. The 2 spec commits ride along when it merges (user OK'd).
- **Prod deploy pending** (user asked for it): after merge, rsync-redeploy bot to minesupply + `docker compose build && up -d` + **re-run setWebhook with `allowed_updates=["message","callback_query"]`** (exact curl in `deploy/README.md` §8). Rsync commands with excludes: 2026-07-04 block in PROGRESS.md. Confirm with user before touching the VPS; NEVER rsync over `deploy/bot/.env`.
- Local branch `feat/family-finance-mvp` still carries the 11 feature commits unpushed — after PR #6 merges, `git reset --hard origin/feat/family-finance-mvp` (do NOT push it directly).

## Don't-forget context

- **User's global rule:** written plans need explicit approval, then ASK which execution mode (subagent vs inline). This session: approved + subagent-driven.
- **Spec's trickiest requirement:** migration 0015 must ALSO re-gate `create_installment_purchase` (0002) to the 0011 gate pattern (`auth.uid() is not null AND not member`) or the bot's null-uid service-role client cannot persist installments. Safe post-0012 (anon EXECUTE revoked).
- Transfer-check narrowing: both-instruments ONLY where `bill_month IS NOT NULL`; caixinha transfers keep XOR. Web payment-edits need a parcela-style guard on transfer rows. Delete of a settle row = intentional undo.
- Deferred minors from the buttons feature live at the end of `.superpowers/sdd/progress.md` (concurrent-tap idempotency key, small test gaps) — fold into future work, not blockers.
- User skills: `codex-review` / `codex-implementation` / `codex-computer-use` (in `~/.claude/skills/`); user may ask to delegate implementation to codex.
- pt-BR for ALL user-facing bot copy; no new npm deps in the bot (rejected grammY).

## Verification commands

```bash
cd /Users/alvarocarvalho/desenv/personal/alvaro-e-karol/family-finance
git log --oneline -4                      # b177bbc, 5c45bc5, dad3423, ed5f4f3
gh pr view 6 --json state,mergeable       # OPEN until user merges
pnpm typecheck && pnpm test               # gate green (bot 175, db 60)
tail -20 .superpowers/sdd/progress.md     # buttons feature ledger, DONE + PR #6
```

## Reading order for the next agent

1. `thoughts/notes/PROGRESS.md` — trailing "Current state", then the 2026-07-06 section.
2. This doc — next action + constraints.
3. `docs/superpowers/specs/2026-07-06-bot-card-installments-and-bill-payment-design.md` — the spec to plan from.
4. `thoughts/tech-debt.md` — open items (nothing resolved this session).
